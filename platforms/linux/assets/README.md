# Launcher artwork

`1password.svg` is from theSVG:
https://cdn.jsdelivr.net/gh/glincker/thesvg@main/public/icons/1password/default.svg

Selected by the user on September 9, 2026. The supplied npm CDN URL returned
404, so this copy uses the same library's GitHub CDN path. The launcher uses
an absolute path to its local copy so the Qogir theme cannot override it.
